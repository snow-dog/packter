import { createTheme } from '@mui/material/styles'

// Create a theme instance.
export const Theme = createTheme({
	palette: {
		primary: {
			main: '#556cd6',
		},
		secondary: {
			main: '#19857b',
		},
		error: {
			main: '#d32f2f',
		},
	},
})
